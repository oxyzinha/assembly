    <script>
        function scrollToNextImage(nextImageId) {
            var nextImage = document.getElementById(nextImageId);
            nextImage.scrollIntoView({ behavior: 'smooth' });
        }
    </script>